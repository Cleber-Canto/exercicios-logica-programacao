const fs = require('fs');

fs.rename('arquivoRenomeado.txt', 'subdiretorio/arquivoMovido.txt', err => {
  if (err) {
    console.error(err);
    return;
  }
  console.log('Arquivo movido com sucesso!');
});
